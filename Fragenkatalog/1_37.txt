/* Diese Definitionen muss man sich einfach merken */

double func1(); ist eine Funktion, die einen double-Wert liefert.
double *func2(); ist eine Funktion, die einen Zeiger auf einen double-Wert liefert. //wichtig
double (*func3)(); ist ein Zeiger auf eine Funktion, die einen double-Wert liefert.
double *(*func4)(); ist ein Zeiger auf eine Funktion, die einen Zeiger auf einen double-Wert liefert. //Grundgütiger
