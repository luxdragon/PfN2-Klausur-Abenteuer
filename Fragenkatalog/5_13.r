# Die rpois(a, b) R-Funktion generiert zufällige Poisson-Werte und liefert das Ergebniss zurück.
# Die Funktion hat zwei Argumente: a... die Anzahl der Beobachtungen und
# b die erwartete Ereignisse pro Einheitsintervall (ausgedrückt durch der durchschnittlichen Anzahl)

# mu ist hier ein Lambda-Parameter
