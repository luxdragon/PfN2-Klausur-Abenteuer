# Lösungen von M. mit Kommentaren von L.
a = c(1.0, 2.0, 3.0)
print(typeof(a))

#Ausgabe:
#"double"

#Aber ist das nicht ein Double-Vektor? (Siehe r1and2 Seite 15)
# Ich verstehe nicht warum hier = verwendet wird, sollen wir nicht (Siehe r1and2 Seite 13)
# nur <- benutzen?
