/* Printf() gibt Char-Streams auf der Stdout-Konsole aus, zum Beispiel:
printf(„Das ist eine Ausgabe: %d“, zahl); //Dies wird dann in der Konsole angezeigt.

fprtinf() gibt Char-Streams in eine Datei aus, zum Beispiel:
fprintf(FILE *ptr, „%d“, zahl); //typischerweise ist FILE *ptr gleich stderr und wird für Fehlermeldungen benutzt */
